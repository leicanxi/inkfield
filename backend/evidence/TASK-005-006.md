# TASK-005 / TASK-006 验证记录

验证日期：2026-07-16（Asia/Shanghai）

## 已实现

- `projects`、`stages`、`milestones`、`project_closure_snapshots` ORM 与第三段 Alembic 迁移。
- 项目/路线 revision、项目内 task event 水位、截止日期口径、终态原因和同租户继任项目复合外键。
- 每项目至多一个 active Stage、一个 active Milestone；当前路线 order 的部分唯一索引。
- advanced/superseded/closed Stage 与 Milestone 的数据库冻结触发器；关闭快照 UPDATE/DELETE 拒绝触发器。
- draft/planning/active/paused/completed/closed/archived 集中状态机及合法转换表。
- 创建、只读查询、重命名、pause、resume、显式 complete、archive 和确定性 deadline close 服务。
- pause/resume/complete/archive/deadline-close 的事务内协作接口，供容量、任务、PlanningRun 和 Proposal 后续切片接入。
- 路线读模型严格拆分 current、冻结 history 和 tentative future，并进行集合互斥校验和稳定排序。
- `/api/v1/projects` 既定 REST 接口；创建和状态命令使用用户范围的 `Idempotency-Key`。

## 已通过

| 检查 | 证据 |
|---|---|
| 格式与静态 | `ruff format --check`、`ruff check` 通过 |
| 类型检查 | `mypy app tests`，85 个源文件无问题 |
| 单元/契约 | `pytest`，50 项通过 |
| 状态机 | 18 个合法转换用例及非法转换无变更用例通过 |
| 竞争模拟 | 相同 revision 的 pause/complete 仅一个 CAS 提交成功 |
| 路线保护 | ORM 元数据断言 active 部分唯一索引、租户键；迁移触发器静态审计通过 |
| API 契约 | 既定 Project/Route/Closure/pause/resume/complete/archive 路径存在于 OpenAPI |
| 迁移 | Alembic offline SQL 通过 3 个 revision，包含 4 张项目路线表、部分索引和 3 个不可变触发器 |
| 架构边界 | `python scripts/check_architecture.py`，7 个 domain 文件通过 |

## 尚未关闭

- `CHECK-005-B` 的数据库实际唯一约束/复合外键回滚，以及 `CHECK-006-C` 的 PostgreSQL 行锁竞争，需要 Docker/PostgreSQL 环境。
- pause/resume/complete 的容量、WeekPlan、Task、PlanningRun 和 Proposal 原子副作用将在对应后续 TASK 接入；当前已固定事务协作接口，不伪造尚不存在的表。
- 因上述真实集成证据尚缺，checklist 状态保持未勾选。
