# TASK-003 / TASK-004 验证记录

验证日期：2026-07-16（Asia/Shanghai）

## 已实现

- `users`、`user_identities`、`user_devices`、`auth_sessions`、`refresh_tokens`、`user_preferences`、`api_idempotency_records` ORM 与 Alembic 迁移。
- 身份/设备/会话复合租户外键、refresh token 哈希与轮换链、每会话单一 active token 部分唯一索引。
- 微信身份 Provider 注入边界；未配置环境安全返回 `AUTH_PROVIDER_UNAVAILABLE`，测试不访问外网。
- HS256 access token 携带 user/session/auth/session version；每次敏感认证重新检查数据库版本和状态。
- refresh 原 token 与 Session 同事务锁定；旧 token 重放撤销整个会话，其他设备不受影响。
- 偏好更新使用 `preference_revision` CAS；幂等记录按 `(user_id, key)` 隔离并保存规范 method/path/hash、租约和首次响应。
- 请求级数据库事务统一提交/回滚；仓储在已有认证事务内复用事务，避免嵌套 `begin()`。

## 已通过

| 检查 | 证据 |
|---|---|
| 静态检查 | `python -m ruff check .` |
| 格式检查 | `python -m ruff format --check .` |
| 类型检查 | `python -m mypy app tests`，74 个源文件无问题 |
| 单元/契约 | `python -m pytest`，24 项通过 |
| 架构边界 | `python scripts/check_architecture.py`，5 个 domain 文件通过 |
| 迁移静态验证 | Alembic offline SQL 成功生成 2 个 revision、7 张身份表；约束名审计通过 |

覆盖场景包括：正常登录/轮换、仅保存 refresh 哈希、旧 token 重放、两请求竞争同一 token、其他设备隔离、偏好 CAS 与并发写、幂等重放、key/body 冲突、不同用户复用同名 key、认证路由/OpenAPI 和安全失败响应。

## 尚未关闭

- `CHECK-003-*`、`CHECK-004-*` 中要求的 PostgreSQL 并发锁、唯一索引和事务回滚集成证据，需要 Docker/PostgreSQL 可用后执行。
- 当前没有 Docker 命令，因此 checklist 状态保持未勾选；离线 DDL 与内存竞争测试不能替代真实数据库竞争测试。
